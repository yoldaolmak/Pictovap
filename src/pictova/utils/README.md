# Pictovap Utils

Small shared utility helpers used across Pictovap.

## Modules

- `config.py`: Project root resolution, `.env` loading, and shared path
  helpers (Visual Memory DB path, working directory, etc.).
